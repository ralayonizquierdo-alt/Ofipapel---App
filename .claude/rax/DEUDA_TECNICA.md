# Deuda técnica — RAX

Formato por entrada: síntoma → causa → riesgo si no se corrige → esfuerzo →
prioridad (Impacto/Riesgo/ROI → score, ver fórmula en
`project-manager/SKILL.md`). Estado `resuelto` se mantiene en la tabla (no
se borra) para conservar el historial.

| ID | Síntoma | Causa | Riesgo si no se corrige | Esfuerzo | I / R / ROI → score | Estado |
|---|---|---|---|---|---|---|
| DT-01 | `.gitignore` raíz ignoraba **toda** la carpeta `joe-app/` | Regla `joe-app/` en vez de `joe-app/node_modules/` + `joe-app/dist/` (a diferencia de `alquileres/`, que sí lo hace bien) | Cualquier fichero nuevo creado dentro de `joe-app/` queda invisible para git de forma silenciosa: no aparece en `git status`, se puede perder trabajo sin darse cuenta | Baja (1 línea) | 4 / 1 / 3 → 13 | **Resuelto** (2026-07-10, ver `DECISIONES.md`) |
| DT-02 | Conviven `index.html` (minúscula) e `Index.html` (mayúscula) en la raíz | Verificado en `build.sh`/`netlify.toml`: `index.html` es un redirect (`location.replace('Index.html')`) intencional, necesario porque los hosts estáticos sirven `index.html` por defecto pero el sitio real es `Index.html` | Ninguno — no es deuda real, es un shim de compatibilidad que funciona como se espera | — | — | **Resuelto** (2026-07-12, confirmado por lectura de código, no era un bug) |
| DT-03 | Dos implementaciones paralelas de agente de WhatsApp (Meta Cloud API y Twilio) sin documentación de cuál es la canónica | Se añadió Twilio como alternativa (commit "Add Twilio WhatsApp webhook as alternative to Meta Cloud API") pero no hay registro de cuál está realmente conectada en producción | Riesgo de mantener/depurar el webhook equivocado, o de que ambos respondan a la vez si los dos están configurados en Meta/Twilio a la vez | Baja (es una decisión + documentarla) | 3 / 2 / 2 → 8 | **Resuelto** — decisión del propietario: Meta Cloud API es la canónica, se eliminó `twilio-webhook.js` y toda referencia a Twilio (ver `ROADMAP_TECNICO.md` RT-01) |
| DT-04 | Sin tests automatizados | Nunca se han escrito | Un cambio puede romper una app sin que nada lo detecte salvo el uso real | Alta (no abordado) | 3 / 2 / 1 → 7 | Abierto — el linting/build sí está cubierto ahora (ver DT-04b), tests no |
| DT-04b | Sin CI de verificación antes de desplegar | No existía workflow de verificación, solo de despliegue (`pages.yml`) | Un error de build en `alquileres`/`joe-app` solo se detectaba al fallar el deploy en Netlify | Media | 3 / 2 / 2 → 8 | **Resuelto** — `.github/workflows/ci.yml` corre lint+build en cada PR/push a `main` |
| DT-05 | Esquemas de Supabase no centralizados | `alquileres/` (Supabase, ahora descartado) y `joe-app/` tienen cada uno su propio `supabase-schema.sql` suelto | Ya no aplica a `alquileres/` (pasó a Firebase); sigue aplicando a `joe-app/` en solitario | Baja | 2 / 2 / 1 → 3 | Reducido de alcance (2026-07-12) — solo `joe-app/` |
| DT-06 | `Index.html` llamaba a la API de Anthropic directamente desde el navegador con la API key personal de cada usuario en claro (localStorage + header `anthropic-dangerous-direct-browser-access`) | Diseño inicial del asistente de IA, sin proxy server-side | Cualquiera con acceso al navegador o al tráfico de red podía capturar la API key personal del usuario | Media | 5 / 1 / 4 → 17 | **Resuelto** (2026-07-12) — proxy `netlify/functions/chat-assistant.js`, key solo en servidor |
| DT-07 | `joe-app` tenía RLS desactivado en Supabase con grant total a `anon` | Decisión original para evitar fallos silenciosos sin login real (ver histórico en PR #21/#24) | Cualquiera con la clave `anon` pública (visible en el bundle JS) podía leer/escribir/borrar todos los datos sin pasar por la app | Baja (ya escrito) | 5 / 1 / 4 → 17 | **Resuelto** (2026-08-19) — RLS + política `authenticated` + sesión anónima. El propietario confirmó "Allow anonymous sign-ins" activado en Supabase y ejecutó `joe-app/supabase-schema.sql` en el SQL Editor (RLS habilitado, políticas `authenticated_all_*` creadas, `anon` revocado). Blindaje real activo, no solo en código |
| DT-08 | `alquileres` (Firebase Firestore) no tenía ninguna regla de seguridad versionada ni conocida — mismo tipo de exposición que DT-07 pero en el backend nuevo | No existía `firestore.rules` en el repo; las reglas por defecto de un proyecto Firebase nuevo suelen ser abiertas o cerradas sin control fino | Datos reales de 8 apartamentos (reservas, pagos, reparaciones) expuestos con solo la clave pública si las reglas por defecto eran abiertas | Baja (ya escrito) | 5 / 1 / 4 → 17 | **Resuelto** (2026-08-07) — `alquileres/firestore.rules` exige sesión + `ensureAnonSession()`; el propietario confirmó el proveedor "Anonymous" ya activado en Firebase Console (Authentication → Sign-in method) y publicó las reglas del repo en Firestore Database → Rules. Blindaje real activo, no solo en código |
| DT-09 | El login de `alquileres` (Luis/Rober) y el de `Index.html` son 100% client-side: no protegen los datos, solo la interfaz — cualquiera que hable directamente con Supabase/Firestore usando la clave pública los salta por completo | Ninguna de las dos apps tiene autenticación de usuario real (Firebase Auth / Supabase Auth con identidad), solo una contraseña comparada en el navegador | Con DT-06/DT-07/DT-08 resueltos el acceso directo ya exige una sesión, pero sigue siendo una sesión anónima compartida, no una identidad real por persona | Alta (requiere rediseñar el login) | 3 / 3 / 2 → 9 | Abierto — deliberadamente fuera de alcance de este sprint, es un cambio de producto, no un parche |
| DT-10 | `alquileres/package.json` declara `@supabase/supabase-js` como dependencia pero no se usa en ningún fichero desde la migración a Firestore | Dependencia residual del intento de backend anterior a Firestore | Ninguno funcional; aumenta el tamaño del bundle/instalación sin motivo | Baja (quitar la línea + `npm install`) | 1 / 1 / 1 → 2 | Abierto — cambio trivial pero toca `package.json`/lockfile de una app en uso; se deja documentado en vez de tocarlo sin pedir turno |
| DT-11 | `alquileres/LoginScreen.tsx` comparte **el mismo hash de contraseña por defecto** que `Index.html` (mismo dato que DT-09 describe, pero aquí el hallazgo concreto: es el mismo valor exacto, no solo "ambos son client-side") entre sus dos usuarios (Luis/Rober) | Mismo valor de hash copiado, no es coincidencia | Si se filtra o se rompe una, se rompen las dos a la vez | Alta (requiere decisión de negocio sobre credenciales) | 5 / 4 / 3 → 15 | **Resuelto en código** (2026-08-19, rama `feat/firebase-auth-fix-alquileres`) — `alquileres` deja de comparar hashes en el navegador y pasa a Firebase Auth real (`src/lib/auth.ts`): Luis y Rober entran con correo interno (`luis@alquileres.internal`, `rober@alquileres.internal`, compuesto por la app y nunca mostrado) más contraseña propia, así que ya no comparten hash con `Index.html` ni entre sí. `passwordAuth.ts` eliminado; `ChangePasswordModal` cambia la contraseña real en Firebase y `Repairs` verifica con `reauthenticateWithCredential` en vez del hash. **Hallazgo de camino**: las reglas de Firestore sí estaban desplegadas (sin sesión, `PERMISSION_DENIED` verificado), pero solo exigían `request.auth != null` y la app abría una sesión anónima al arrancar — y una sesión anónima la puede crear cualquiera con la apiKey pública del bundle: comprobado leyendo las 79 reservas sin pasar por el login. La app ya no abre sesión anónima y no hace ninguna petición a Firebase antes del login (verificado: 0 peticiones). **Pendiente del propietario**: crear las dos cuentas en Firebase Console → Authentication → Usuarios, y solo después desplegar `firestore.rules` (que ya exige `sign_in_provider == 'password'`), porque desplegarlas antes deja a todos fuera |
| DT-12 | `joe-app` y `alquileres` no tenían `eslint.config.js` (`joe-app`) o tenían errores reales nunca detectados (`alquileres`) porque nadie había podido ejecutar `npm run lint` con éxito hasta añadir el CI | Ausencia histórica de verificación automatizada | Errores reales de código (Fast Refresh roto, variables sin usar, bloques vacíos) pasaban desapercibidos indefinidamente | Baja-media (ya corregido) | 3 / 1 / 2 → 9 | **Resuelto** — 9 errores reales corregidos entre las dos apps durante la consolidación de ramas, ver `DECISIONES.md` |
| DT-13 | `react-hooks/set-state-in-effect` — casos preexistentes en `alquileres` (`Reservations.tsx` ×2, `MigrateLocalData.tsx` ×1) | Patrón de cálculo automático de precios y de arranque de migración escrito antes de que la regla existiera | Ninguno funcional observado; el patrón puede causar renders en cascada innecesarios | Media (requiere tocar lógica de negocio activa) | 2 / 2 / 1 → 3 | Abierto, degradado a warning con TODO en `alquileres/eslint.config.js` — no se ha tocado la lógica |
| DT-14 | Sin observabilidad de errores en ninguna de las 3 apps React ni en las funciones de Netlify | Nunca se ha configurado un servicio de error tracking | Un fallo en producción solo se detecta si un usuario lo reporta a mano | Media (`@sentry/react` + `ErrorFallback.tsx`, código ya escrito y verificado en la rama `claude/autonomous-dev-environment-8obtv2`, PR #61) | 3 / 2 / 2 → 8 | Pendiente, no rescatado a propósito — el intento anterior de Sentry en `netlify/functions` ya se revirtió una vez por romper el deploy; si se retoma, probar aparte primero |
| DT-15 | `netlify/functions/marketing-engine-run.js` (el único punto real de conexión con `app.html`) llamaba a `creative-engine/index.js#runCreativePipeline` — el pipeline SIMPLE (asset-pipeline + variant-generator), no a `creative-engine/creative-lab/index.js#runCreativeLab` — el pipeline con Art Direction Engine, Layout Intelligence, Design Director y Component Library construido y perfeccionado en la rama "Cierre de arquitectura" (sprints "Art Direction Engine" a "Fase 7 — Conexión OpenAI") | Ambos pipelines se construyeron en momentos distintos con entradas/salidas de forma compatible pero no idéntica; nadie había vuelto a `marketing-engine-run.js` desde que `creative-lab/` existe para hacer el cambio de uno a otro | Ninguna campaña generada desde `app.html` en producción pasaba por las 4 familias oficiales, los 18 patrones editoriales, el Design Director ni el resto de ese trabajo — toda esa sofisticación solo era alcanzable desde CLI/scripts sueltos, no desde el flujo real que usa el propietario | Media (cambiar una llamada + adaptar la lectura de `variant`/`creativeResult` al shape de `labResult.winner`, sin tocar ninguno de los dos pipelines en sí) | 5 / 2 / 4 → 17 | **Resuelto** (2026-08-01) — `marketing-engine-run.js` llama a `runCreativeLab`; `netlify.toml` ahora incluye `creative-engine/**` (no estaba, bug latente aparte); `reference-library/service.js#findRelevantReferences` completaba con `general` solo si había <2 matches directos, dejando a categorías poco pobladas sin variedad suficiente para 8-12 conceptos — corregido a completar hasta `limit`. Verificado con el handler real: 4/4 familias oficiales ganan como pieza final aprobada (Design Director `passed:true`) en distintas ejecuciones; pipeline antiguo (`runCreativePipeline`) ya no se invoca desde ninguna ruta de producción. Conexión real a OpenAI Images (proveedor `openai-images`) verificada con prueba end-to-end mockeada — activo con `OPENAI_API_KEY` en el entorno |
| DT-16 | El Lambda real de Netlify no tiene el paquete `playwright` disponible — `07-maquetador` (agente del pipeline de 8 de `marketing-engine`) y `creative-lab/layout-composer` (que renderiza la pieza final de `creative-lab` con el mismo `design-studio/scripts/render-html.js`) fallan con `Cannot find module 'playwright'` en cuanto se ejecutan de verdad en producción | Ya era un bloqueante conocido y documentado en el propio comentario de cabecera de `marketing-engine-run.js` e `INTEGRATION.md` ("Playwright/Chromium en Lambda, no resuelto aún") desde antes de esta sesión, pero nunca se había confirmado con una ejecución real en producción hasta el sprint "Fase 7 — Conexión OpenAI" (2026-08-01) | **Ninguna campaña real completa hasta pieza final** (ni con el proveedor `simulated` ni con `openai-images`) — el pipeline entero falla antes de llegar a `status:'completed'`, así que el bloque que llama a `creative-lab`/OpenAI en `marketing-engine-run.js` nunca llega a ejecutarse. Bloquea también la validación real de DT-15 en producción, aunque el código de DT-15 en sí está verificado (mock) | Media-alta (empaquetar un Chromium compatible con Lambda — típicamente `@sparticuz/chromium` + `playwright-core` en vez de `playwright` completo — en la función `marketing-engine-run`, y adaptar `render-html.js` para usar ese binario cuando `AWS_LAMBDA_FUNCTION_NAME` esté presente) | 5 / 2 / 3 → 10 | **Resuelto en código** (2026-08-01) — `netlify/functions/package.json` nuevo (único `package.json` de npm de toda la función, el resto del repo sigue sin uno en la raíz) con `@sparticuz/chromium` + `playwright-core`; `render-html.js#getBrowser()` detecta `AWS_LAMBDA_FUNCTION_NAME`/`LAMBDA_TASK_ROOT` y usa ese Chromium en vez del de las sesiones en la nube; `NODE_PATH_FOR_PLAYWRIGHT` (en `07-maquetador/config.js` y `layout-composer/config.js`) ahora prueba también `netlify/functions/node_modules`; `netlify.toml` empaqueta esos `node_modules` vía `included_files` + `external_node_modules`. Verificado localmente simulando el entorno Lambda (`AWS_LAMBDA_FUNCTION_NAME` + `NODE_PATH`): pipeline completo de 8 agentes + `creative-lab`/OpenAI (mock) terminan en `status:'completed'` con PNG real generado por el binario real de `@sparticuz/chromium`. **Confirmado en producción real** (2026-08-01/02): tras 3 rondas más de fallos de empaquetado (módulo no encontrado → dependencia transitiva `follow-redirects` no encontrada → resueltos con `build.sh` instalando `netlify/functions/` antes del bundling y `included_files` empaquetando `node_modules/**` completo), logs reales de `marketing-engine-run` muestran ejecuciones de 36-39s con memoria 700-770MB y sin error — Chromium real generando la pieza final en Lambda. Bloqueante cerrado; el problema restante es DT-17 |
| DT-17 | `marketing-engine-run` (función síncrona) siempre falla desde el cliente con `504 Inactivity Timeout` cuando el proveedor real (`openai-images`) está activo, aunque la función termine bien por detrás | Una generación real (OpenAI Images + render de Chromium + composición de `creative-lab`) tarda 36-39s de forma consistente incluso ya al mínimo posible (`CREATIVE_LAB_SHORTLIST_SIZE=1`, `MAX_RETRIES=1`, `OPENAI_IMAGES_QUALITY=low`) — más que el límite del proxy síncrono de Netlify que da la cara al cliente (~26s, distinto e independiente del límite de ejecución de la propia función Lambda, que sí permite hasta 60s). Confirmado con logs reales: ejecuciones de 36147ms/39141ms sin ningún error, mientras el cliente recibía igualmente el 504 | Con el proveedor real activo, **ninguna generación llega nunca al cliente** por esta vía — el pipeline funciona y genera la pieza (confirmado en logs), pero la respuesta se pierde siempre. Bloquea la validación real de Fase 7 (ver `DECISIONES.md`) y el uso real desde `app.html` en cuanto se conecte a este endpoint | Media (patrón Background Function + polling, sin tocar el pipeline en sí) | 5 / 3 / 4 → 22 | **Resuelto en código** (2026-08-02) — nueva función `marketing-engine-run-background.js` (mismo pipeline, sufijo `-background` = Netlify la ejecuta de forma asíncrona hasta 15 min, sin el límite de 26s) que escribe el resultado final en Netlify Blobs (`@netlify/blobs`, nueva dependencia) bajo la clave `trackingId` que envía el cliente; `marketing-engine-status.js` (función síncrona ligera, sin el problema de tiempo) lee esa misma clave para servir el resultado cuando esté listo. `marketing-engine-run.js` (síncrona) se mantiene sin cambios para pruebas rápidas con el proveedor `simulated`. Verificado localmente con `@netlify/blobs` mockeado (no hay contexto real de Blobs fuera de Netlify): ambas funciones ejecutan su lógica completa correctamente. **Confirmado en producción real** (2026-08-02): primera generación real de extremo a extremo (`providerId:"openai-images"`, `status:"approved"`, Design Director 85/100 banda "excelente", PNG 1080×1920) recibida por el cliente vía polling. Fase 7 cumplida — ver DECISIONES.md |
| DT-18 | La pieza final generada con el patrón `lifestyle-premium` (primera generación real, DT-17) tiene varios defectos visuales que el Design Director no penaliza: título "Ventilador Muvip" duplicado (aparece dos veces), logo y botón CTA "Míralo en tienda" solapando la foto del producto, texto de contacto cortado en el borde inferior, y un botón/CTA inferior con dos textos superpuestos e ilegibles | El patrón `lifestyle-premium` (u otro componente compartido que usa) tiene un fallo real de posicionamiento/duplicación de elementos; `design-director/criteria.js` (concretamente `limpieza`, que revisa solapes) no lo detectó — dio 8/8 y "sin solapes entre elementos en primer plano" pese a los solapes reales visibles | La calidad visual real de la pieza es menor de lo que indica su puntuación (85/100 "excelente") — el Design Director no es una garantía fiable de que la pieza esté libre de estos defectos concretos, y podría estar aprobando otras piezas con el mismo problema sin que nadie lo note salvo revisión manual | Media-alta (depurar `lifestyle-premium` en `component-library`/`layout-composer` para la duplicación/solape de título y CTA, ajustar el margen del footer de contacto, y revisar por qué `criteria.js#limpieza` no detecta este caso concreto) | 3 / 2 / 3 → 12 | Abierto — identificado por revisión visual manual del propietario sobre la primera generación real (2026-08-02), pendiente de priorizar; "ya iremos perfilando" (decisión explícita de no atacarlo en esta sesión). **Confirmado sistemático** (mismo día): el título duplicado/garbled y el CTA con texto solapado aparecen igual en 3 generaciones reales (`test-2`, `test-3` objective `sorprender`, `test-4` objective `resolver-problema`) — no es un defecto puntual de una pieza, es del componente/patrón compartido en sí. **Corrección**: se afirmó inicialmente que `test-3`/`test-4` habían salido con familias oficiales distintas por el tono del titular generado por OpenAI dentro de la foto (texto alucinado, no fiable como señal) — el propietario, revisando las 3 imágenes con más detalle, observa que estructuralmente (encuadre, fondo, posición de logo/título/CTA/iconos) `test-3` y `test-4` son la misma plantilla. Nunca se confirmó con los campos reales (`creative.officialFamily`/`patternId`) porque el propietario no llegó a pegar ese JSON — **queda abierto y sin verificar** si `objective` cambia de verdad la familia elegida por `selectPattern()`, no dar por bueno hasta confirmarlo con los campos reales. **Corrección de diagnóstico (2026-08-02, experimento "Fotografía Base")**: comparando la foto cruda del proveedor (sin nada compuesto) contra la pieza final compuesta con el mismo patrón `lifestyle-premium`/Lifestyle, la pieza final NO mostró título duplicado ni CTA con texto solapado esta vez — solo el título real y el CTA real, una vez cada uno. Esto indica que el diagnóstico original (bug de `layout-composer`/`component-library`) era **incorrecto**: lo más probable es que OpenAI estuviera alucinando sus propios elementos falsos de "anuncio" (títulos y botones inventados) dentro de la propia fotografía en los intentos anteriores, y `layout-composer` los superponía con sus elementos reales — de ahí la apariencia de "duplicado". El modo normal de campaña (incluso tras FASE 8) no prohibía estos elementos con la suficiente literalidad; el prompt de "Fotografía Base" sí, con una lista exhaustiva (botón/CTA/cartel/rótulo/elemento gráfico superpuesto), y el resultado salió limpio. **Recomendación real**: reforzar `NEGATIVE_PROMPT_TERMS`/`sections/from-concept.js` del modo normal con la misma literalidad exhaustiva de `base-photography.js`, en vez de tocar `layout-composer`. Persiste sin resolver, incluso en modo "Fotografía Base": texto falso grabado en la tapa central del ventilador (p.ej. "FROCT4G") — gpt-image-1 parece añadir compulsivamente marcas/etiquetas falsas sobre superficies de producto pese a la prohibición explícita y literal; posible límite real del modelo, no solucionable solo con prompting |
| DT-21 | XSS almacenado en los HTML monolíticos nunca auditados: `fichaje.html` (motivo de corrección, "corregido por" y nombres de empleado), `vacaciones.html` (nombres de empleado, etiquetas de periodos bloqueados, unidades), `app.html` (título/estilo de campaña) e `importacion-pedidos-proveedores.html` (los 15 campos extraídos del PDF del proveedor, insertados en `value="..."` sin escapar) | Interpolación directa de texto libre en `innerHTML` sin ninguna función de escape — ninguno de los 6 ficheros tenía una (salvo `falcontrol.html`, que sí tenía `esc()` pero no la aplicaba en el log de depuración) | El más grave es `fichaje.html`: el motivo de corrección lo escribe una persona y se pinta en la vista de gerencia, que es justo donde vive el token de administración de Firebase en `localStorage` — un XSS ahí permite robarlo. En `importacion-pedidos-proveedores.html` la entrada es **externa** (PDF de un proveedor), no solo interna: una descripción con comillas rompe el atributo e inyecta un manejador de eventos | Baja (helper `escapeHtml()` + aplicarlo en cada punto) | 4 / 3 / 4 → 19 | **Resuelto** (2026-08-19) — `escapeHtml()` añadido a `fichaje.html`, `vacaciones.html`, `app.html` e `importacion-pedidos-proveedores.html` y aplicado en todos los puntos con datos de usuario; `falcontrol.html` usa su `esc()` ya existente en el log. `canarias-ink.html` revisado sin hallazgos (catálogo hardcodeado, el buscador no se refleja en el DOM). Los usos restantes sin escapar son seguros y verificados uno a uno: `showToast()`/`textContent`, `confirm()` y el texto plano de exportación a WhatsApp |
| DT-29 | **Al cerrar las reglas de `ofipapelvv` se rompió la pantalla de fichar**: `fichaje.html` carga la plantilla de personal al arrancar, antes de cualquier login, y lo hacía abriendo una sesión **anónima** contra `ofipapelvv` para leer `ofipapel_vacaciones/shared`. Comprobado en vivo (2026-08-28): esa lectura devuelve `403 PERMISSION_DENIED`, y como `loadPeople()` capturaba el error y seguía, la lista se quedaba solo con los 3 de gerencia — **el personal no podía fichar** | `alquileres/firestore.rules` se escribió pensando solo en Alquileres, pero su `match /{collection}/{docId}` cubre **todo** el proyecto — y `ofipapelvv` está compartido con `vacaciones.html` y con esta lectura de `fichaje.html`. `vacaciones.html` no se vio afectado porque ya usaba contraseña real; `fichaje.html` sí, porque usaba sesión anónima. Nadie contó quién más leía ese proyecto antes de cerrarlo | Registro horario caído para toda la plantilla, en una app cuyo propósito legal es justamente dejar constancia de la jornada (RD-ley 8/2019). Es además el efecto secundario exacto que DT-23 advertía: cerrar reglas antes de migrar quien las usa deja gente fuera | Baja-media (proxy server-side, ya escrito) | 5 / 4 / 5 → 24 | **Resuelto** (2026-08-28, PR #199) — verificado en producción con peticiones reales: la función devuelve los **36 empleados en 11 departamentos**, `fichaje.html` en vivo ya trae el código nuevo, y el control de acceso responde 403 sin cabecera de origen, 403 desde otro dominio y 405 a un POST. El propietario confirmó en pantalla que la lista de personas vuelve a salir completa. La corrección: nueva función `netlify/functions/plantilla-vacaciones.js`: inicia sesión con contraseña real contra `ofipapelvv` usando credenciales que viven **solo en el servidor** y devuelve únicamente `{id, name, unitId}` de los empleados activos (no el resto del estado de Vacaciones). `fichaje.html` la usa como vía preferente, conserva la anónima como red de seguridad y **cachea la última plantilla buena en `localStorage`**, así que ningún fallo puntual vuelve a vaciar la lista. La cuenta en `ofipapelvv` y las dos variables (`VACACIONES_FIREBASE_EMAIL` / `VACACIONES_FIREBASE_PASSWORD`) las creó el propietario en el sitio `ofipapel` de Netlify. **Lección que queda de aquí, aplicable al cierre pendiente de `ofipapel-fichaje-63ced` (DT-23): antes de cerrar las reglas de un proyecto Firebase, listar TODAS las aplicaciones que leen de él — no solo la que motiva el cambio** |
| DT-32 | **Netlify cobra por despliegue, no por minuto de build** — 15 créditos fijos cada uno, dure 30 segundos o 10 minutos. Con 500 créditos por 5 $, cada despliegue son 0,15 $. El desglose real de la cuenta (2026-09-02) lo deja sin ninguna duda: de 2.683,5 créditos consumidos en el periodo, **2.655 son "Production deploys" (177 despliegues) — el 98,9%**. Todo lo demás junto suma 28,5: compute 14,9, ancho de banda 10,8, peticiones web 2,8 e inferencia de IA 0 | Los tres sitios construían el repositorio entero en cada push, así que 59 pushes se convertían en 177 despliegues facturables. Y el modelo de precios no penaliza builds largos sino builds **frecuentes**, que es justo lo contrario de lo que se estaba optimizando | ~27 $ al mes solo en despliegues, más la suscripción. El propietario llevaba comprando paquetes de 5 $ de forma recurrente sin saber a qué correspondían | Baja — ya aplicado | 4 / 2 / 5 → 19 | **Diagnóstico cerrado y corregido a medias** (2026-09-02). El error de análisis previo, para que no se repita: se midieron los **minutos** de build (178 de los 300 del plan) y se concluyó "no consume tanto", cuando la magnitud facturada era otra. Medir la unidad equivocada llevó a descartar como pequeño justo el problema principal. Corregido con las reglas por sitio de `netlify-ignore.sh` (~177 → ~90 despliegues). Lo que falta es de método, no de código: **agrupar el trabajo y subir una vez por sesión** en vez de commit a commit, que llevaría a ~30 despliegues al mes |
| DT-30 | **El repositorio es público** (`"visibility": "public"`, comprobado en la API de GitHub el 2026-08-28) y con él lo es todo lo que contiene — incluido `.claude/rax/AUDITORIA_SEGURIDAD_2026-08.md`, que enumera **qué sistemas están abiertos y por qué**, junto a las URL y claves públicas de Supabase y los identificadores de los dos proyectos Firebase. Verificado leyendo esos ficheros por `raw.githubusercontent.com` sin ninguna credencial | Nadie comprobó la visibilidad del repositorio antes de escribir en él el informe de seguridad. El error es de quien lo redactó — se dio por hecho que `.claude/rax/` era un cuaderno interno cuando en realidad es una página web más | Mientras Finanzas y Fichaje sigan abiertos (DT-23), ese documento es un mapa de instrucciones para entrar, indexable por buscadores. También quedan públicos los tokens de reserva embebidos en el código (`ofipapel-marketing-2026`, `ofipapel-chat-2026`), aunque esos ya se sabían visibles por ir en HTML estático | Media — dos acciones independientes: cerrar los agujeros (DT-23, es el arreglo real) y decidir la visibilidad del repositorio. Ponerlo en privado **apaga GitHub Pages** en cuenta gratuita, que hoy es una cuarta copia redundante del sitio (DT-31), así que no se pierde nada de valor. Borrar el texto no basta: el historial de git lo conserva | 5 / 4 / 5 → 24 | Abierto — el hallazgo más importante del segundo barrido de infraestructura. La prioridad real sigue siendo DT-23: el documento solo es peligroso porque describe agujeros que siguen abiertos |
| DT-31 | **Cada push a `main` dispara cinco construcciones del mismo repositorio**: tres sitios de Netlify (`ofipapel`, `spontaneous-lebkuchen-60fa41`, `joesworld`), el despliegue a GitHub Pages (`.github/workflows/pages.yml`, que ejecuta el mismo `build.sh`) y el CI (`.github/workflows/ci.yml`, que vuelve a compilar `joe-app` y `alquileres`). El resultado son **cuatro copias vivas del mismo sitio**: las tres de Netlify más `ralayonizquierdo-alt.github.io/Ofipapel---App`, verificada al día — sirve incluso el arreglo de fichaje de hoy | Cada destino se añadió en un momento distinto sin retirar el anterior. GitHub Pages se documentó como "respaldo" de Netlify, pero nunca se revisó si seguía haciendo falta | Coste real solo en Netlify: los otros dos son GitHub Actions y el repositorio es público, así que no consumen minutos de pago. Lo grave no es el gasto sino la confusión: cuatro URL sirviendo la misma aplicación, y cualquiera de ellas puede quedarse atrás sin que nadie lo note — que es exactamente lo que pasó con `ofipapel-fichaje-test` (DT-26) | Baja para Pages (borrar `pages.yml`); media para consolidar Netlify (DT-27, cuidado con el webhook de WhatsApp y la URL registrada en Meta) | 3 / 2 / 4 → 14 | Abierto — detectado en el segundo barrido (2026-08-28). Nota: si se decide poner el repositorio en privado por DT-30, GitHub Pages se apaga solo y este punto se resuelve de paso |
| DT-26 | Existe un sitio Netlify aparte, `ofipapel-fichaje-test`, que sirve públicamente una **copia congelada de `fichaje.html`** (5,4 MB, subida a mano el 22/07/2026 arrastrando el fichero: `deploy_source: "drop"`, `build_id: null`, sin funciones) — **con las claves de los DOS proyectos Firebase reales de producción** (`ofipapel-fichaje-63ced` y `ofipapelvv`) y **sin el arreglo de XSS de DT-21** | Se subió como prueba manual y nunca se borró. Al no tener build ni repo asociado, ninguna corrección posterior le llega jamás: es código muerto que sigue publicado y sigue apuntando a datos vivos | No consume minutos de build (por eso no apareció en el análisis de coste), pero es superficie de ataque real: el XSS almacenado corregido en DT-21 sigue explotable aquí, y desde esta copia se alcanzan los fichajes reales y la plantilla de Vacaciones/Alquileres. Cada corrección de seguridad del repo la deja más desfasada | Baja — mover a la gente a `https://ofipapel.netlify.app/fichaje.html` y luego borrar el sitio (Site settings → Danger zone → Delete site). No hay nada que rescatar en él | 5 / 4 / 5 → 24 | Abierto — y **más grave de lo que se documentó al detectarlo**. Se clasificó como "zombi" dando por hecho que no lo usaba nadie; el propietario cree que es justo la URL que usa el personal para fichar. Si es así no es un zombi, es **una copia de producción sin mantenimiento**, que es peor. Comprobado en vivo (2026-08-28): sirve una sola URL (`/fichaje-1.html`; la raíz es 404) y le faltan, frente a la versión del repo, `escapeHtml` (DT-21), las geocercas completas (`loadGeofences`, `haversineMeters`, `isOutsideGeofence`, `persistGeofences`), el cambio de departamento (`effectiveUnitId`, `setPersonUnitOverride`) y las alertas (`renderAlertsBlock`). Lo único que tiene y el repo no es `peopleGroupedByUnit`, renombrada después a `peopleGroupedForClock` — o sea, **nada propio que rescatar**. Además **el arreglo de DT-29 no puede llegarle**: el sitio no tiene funciones ni build, así que `plantilla-vacaciones` no existe ahí y no puede existir. Y está igual de caído que los demás por el mismo 403. Apunta al mismo proyecto Firebase (`ofipapel-fichaje-63ced`), así que mover a la gente a la URL buena no pierde ni un fichaje ni un PIN |
| DT-27 | **Tres sitios de Netlify construyen este mismo repo a la vez en cada push**: `ofipapel`, `spontaneous-lebkuchen-60fa41` y `joesworld`. Confirmado en vivo: mismo commit (`e88f79a`), misma marca de tiempo (`12:45:15`), 17 funciones idénticas cada uno — incluidas dos de 85 MB (`marketing-engine-run` y su versión background) | Los tres sitios se crearon en momentos distintos apuntando al mismo repositorio de GitHub y nunca se consolidaron. Netlify no lo señala en ningún sitio: cada sitio se ve normal por separado | **Es la causa real del gasto de créditos**, no el almacenamiento ni el tráfico (el sitio publicado son solo 9,4 MB): cada push cuesta ×3 builds, y cada PR además ×3 deploy previews. Estimado ~700 min/mes contra los 300 gratuitos | Media — consolidar exige cuidado: el webhook de WhatsApp de Meta apunta a `spontaneous-lebkuchen-60fa41` y la política de privacidad registrada en Meta apunta a `ofipapel.netlify.app`, así que apagar el sitio equivocado rompe el bot o la revisión de Meta | 4 / 3 / 5 → 20 | Parcialmente mitigado (2026-08-28) sin tocar los tres sitios: `netlify-ignore.sh` salta el build entero cuando el commit solo toca documentación, y `build.sh` cachea `netlify/functions/node_modules` (el `npm ci` de `@sparticuz/chromium`, el paso más caro) reinstalando solo si cambia el lockfile. La consolidación de los tres sitios sigue abierta y debe hacerse **con el propietario delante** |
| DT-28 | Proyectos zombis en Vercel: `ofipapel-social-manager-api` y `nextjs-boilerplate` no sirven nada (`DEPLOYMENT_NOT_FOUND` / página de error), pero siguen ocupando sitio en la cuenta | Restos de intentos anteriores nunca borrados. `nextjs-boilerplate` es un `create-next-app` sin relación con el negocio; `ofipapel-social-manager-api` quedó obsoleto cuando el backend pasó a vivir dentro del propio `ofipapel-social-manager` (comprobado: `/api/health` → `{"ok":true}` y `/api/campaigns` → 401 en ese mismo dominio) | Ninguno de coste (Vercel Hobby no cobra por proyecto parado) ni de seguridad (no sirven contenido). El riesgo es de confusión: aparecen en la lista y hacen dudar de cuál es el backend real | Baja (borrar los dos proyectos en el panel de Vercel) | 1 / 1 / 3 → 5 | Abierto — detectado en el barrido de infraestructura (2026-08-28). Solo el propietario puede borrarlos |
| DT-22 | `canarias-ink.html` sigue publicado con datos de contacto de relleno: teléfono de WhatsApp `34600000000`, CIF `B00000000`, domicilio "C/ Ejemplo, 1" y teléfono `928 000 000` | Plantilla inicial del microsite que nunca se completó con los datos reales | No es un problema de seguridad, pero es una web pública de cara a cliente con datos legales falsos (el CIF y el domicilio aparecen en el aviso legal y en la política de devoluciones) — riesgo reputacional y de cumplimiento, no técnico | Baja (sustituir los valores), pero requiere los datos reales | 3 / 2 / 4 → 14 | Abierto — detectado durante la auditoría de los HTML monolíticos (2026-08-19). Necesita los datos reales de Canarias INK, que solo tiene el propietario; ya estaba parcialmente documentado en `SESSION_LOG.md` (2026-07-10) solo para el número de WhatsApp |
| DT-19 | `Index.html` tenía en `SEED_DATA` (código fuente, en claro) 6 registros reales de saldos bancarios (BBVA/Santander/CaixaBank, junio 2026) | Se usó como semilla inicial al crear la app y nunca se limpió cuando la sincronización real con Supabase (`registros`, tabla en la nube) pasó a ser la fuente de verdad | `Index.html` es HTML estático: ese array se descarga al navegador de cualquier visitante ANTES de cualquier login, visible con "ver código fuente" sin contraseña — distinto y más grave que DT-09 (login débil): ahí el problema es la puerta, aquí los datos ya estaban fuera de la puerta | Baja (verificar que Supabase ya tiene los mismos datos, luego vaciar el array) | 5 / 3 / 5 → 23 | **Resuelto** (2026-08-07) — verificado antes de tocar nada: la tabla `registros` de Supabase ya tenía 45 filas (02/06 a 07/08/2026), incluida la fila del 11/06 con los mismos valores exactos que `SEED_DATA` — `loadFromCloud()` ya sustituye siempre este array por los datos reales de la nube en cuanto hay sesión, así que no se perdió ningún dato al vaciarlo. `SEED_DATA` queda como `[]`, documentado en el propio fichero para que no se vuelva a rellenar con cifras reales |
| DT-20 | `Index.html`: el campo "notas" (cajaCierres/cajaBancoRecibos/cajaEfectivo) se insertaba sin escapar en `innerHTML` en 4 sitios (dashboard, histórico, caja) | Interpolación directa de texto libre de usuario en plantillas HTML, sin una función de escape | Con login compartido entre los 4 usuarios (DT-09), cualquiera de ellos podía guardar HTML/JS malicioso en una nota y ejecutarlo en el navegador de cualquier otro usuario que abriera esa vista (XSS almacenado), con acceso completo a los datos financieros y a la sesión de Supabase | Baja (helper `escapeHtml()` + 4 puntos de uso) | 4 / 3 / 4 → 19 | **Resuelto** (2026-08-07) — añadido `escapeHtml()` y aplicado en los 4 puntos donde `.notas` se inserta en `innerHTML`. No toca el CSV export (texto plano, sin riesgo) ni el login compartido (DT-09, decisión de negocio aparte) |
| DT-23 | **Los datos de 4 apps son accesibles sin ninguna contraseña**, comprobado en vivo el 2026-08-19: Finanzas (Supabase "App Bancos") permite **leer Y escribir** los saldos bancarios con solo la clave pública del HTML; Alquileres, Vacaciones y Fichaje (Firestore) se leen abriendo una sesión anónima que Firebase concede a cualquiera que la pida. La plantilla de Vacaciones, con nombres reales de empleados, se lee entera — **saltándose su login por persona, que sí está bien hecho** | Dos causas distintas: (a) en Supabase "App Bancos" la política es `allow_all` para el rol `public` (`USING true WITH CHECK true`) — RLS activado pero sin restringir nada; (b) en Firestore las reglas exigen `request.auth != null`, pero el proveedor "Anonymous" está abierto al mundo, así que "tener sesión" no distingue a nadie: `auth != null` equivale en la práctica a `allow all` | Cualquiera que vea el código fuente (es HTML estático) obtiene la clave y accede. En Finanzas incluye **escritura**: se pueden alterar las cifras de caja. En Vacaciones/Fichaje son datos personales de empleados (RGPD). El login compartido (DT-09/DT-11) es un problema menor comparado con esto: la puerta está cerrada pero la ventana de al lado está abierta | Media-alta — no es un parche: requiere login real por persona (Supabase Auth / Firebase Auth con identidad) en Finanzas, Alquileres y Fichaje, más ajustar las reglas para exigir un proveedor real y no una sesión anónima. `vacaciones.html` **ya lo hace bien** (email+contraseña vía Identity Toolkit) y sirve de patrón a copiar | 5 / 5 / 4 → 24 | **Parcialmente resuelto** (2026-08-28) — **Finanzas cerrada y verificada**: ejecutado `finanzas-rls.sql` (RLS + políticas `to authenticated` + `revoke` a `anon` en `registros` y `app_sync`) y desactivado el alta de usuarios en Supabase. Comprobado con peticiones reales, no con el ajuste declarado: un alta con contraseña válida devuelve `signup_disabled`, y las dos tablas devuelven 401 sin credencial. Antes de tocar nada se verificó que `initSupabase()` solo se llama desde `_completeLogin()`, así que ninguna petición ocurre antes del login — la comprobación que faltó en DT-29. **Sigue abierto Fichaje** (`ofipapel-fichaje-63ced`): su pantalla de fichar usa sesión anónima, así que cerrarlo exige antes sacar esa ruta de la sesión anónima, como se hizo con la plantilla. Era **el hallazgo más grave de toda la auditoría**. Deliberadamente NO corregido en solitario: cerrar las reglas sin migrar antes el login deja al propietario fuera de su propia app de contabilidad. Requiere hacerlo con él delante para poder verificar. Verificado con peticiones reales (una escritura de prueba en `registros`, borrada y confirmada la limpieza acto seguido) |
| DT-24 | `favorite_artists` (Joe's App) seguía con acceso total para el rol `anon` — lectura y escritura sin sesión | La tabla se creó después de `supabase-schema.sql` y nunca se añadió al script, así que el `revoke ... from anon` no la cubrió. Además, las políticas `allow_all` originales (rol `public`) seguían presentes en las otras 6 tablas: hoy inofensivas porque a `anon` se le retiraron los permisos de tabla, pero una trampa si alguien los devolviera | La tabla estaba vacía, así que no hubo fuga de datos; sí permitía que cualquiera insertara filas | Baja | 3 / 2 / 4 → 14 | **Resuelto** (2026-08-19) — migración `proteger_favorite_artists_y_limpiar_politicas_abiertas`: RLS + política `authenticated`, permisos de `anon` retirados, y eliminadas las 6 políticas `allow_all` residuales. Verificado en vivo: las 7 tablas devuelven 401 sin sesión y 200 con la sesión anónima que abre la app — protegido por fuera, funcionando por dentro |
| DT-25 | `MARKETING_ENGINE_TOKEN` no estaba configurada en Netlify, así que el control de acceso añadido en el PR #119 no hacía nada: el endpoint seguía aceptando peticiones de cualquiera | La variable se añadió al código pero nunca al entorno; el código está escrito para no romperse si falta (solo avisa por consola) | Cualquiera con la dirección podía disparar generación de imágenes con IA, con coste real por llamada | Baja (una variable de entorno) | 4 / 2 / 5 → 19 | **Resuelto** (2026-08-19), pero no como se creyó al principio. Se creó la variable en Netlify y se dio por cerrado — **falso**: comprobado en vivo que el endpoint seguía aceptando peticiones, y que un token deliberadamente incorrecto también pasaba, luego la comprobación ni se ejecutaba. Se descartó que fuera un despliegue viejo (el `escapeHtml` del PR #162 sí estaba en producción) y se confirmó que el código nuevo SÍ corre, provocando a propósito el 429 del rate limiting. La variable simplemente no llega a la función y sin acceso a Netlify no se pudo ver por qué. **La causa de fondo era de diseño**: la comprobación solo se aplicaba si la variable existía, y si no, dejaba pasar a todos (*fail open*) — un fallo de configuración silencioso dejaba el endpoint abierto sin que nada lo delatara. Corregido para **cerrar por defecto**: si falta la variable se usa el mismo valor que `app.html` lleva embebido (no añade secreto — es HTML estático — pero impide que quede abierto). La variable, cuando esté disponible, sigue teniendo prioridad. **Verificado en producción** (08:02 UTC): sin token → 401, token incorrecto → 401, token correcto → 400 (pasa la puerta). Cerrado de verdad, comprobado, no supuesto |

## Cómo añadir una entrada nueva

Cada vez que detectes deuda técnica real (no hipotética), añade una fila con
el mismo formato. No la marques `resuelto` hasta que el cambio esté
efectivamente commiteado. Antes de tocar código para resolver una entrada,
clasifica el recurso afectado como Producción / Inactivo con valor / Prueba
(ver `project-manager/SKILL.md`) y respeta esa clasificación.
