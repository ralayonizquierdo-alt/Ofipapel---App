# Deuda técnica — RAX

Formato por entrada: síntoma → causa → riesgo si no se corrige → esfuerzo →
prioridad (Impacto/Riesgo/ROI → score, ver fórmula en
`project-manager/SKILL.md`). Estado `resuelto` se mantiene en la tabla (no
se borra) para conservar el historial.

| ID | Síntoma | Causa | Riesgo si no se corrige | Esfuerzo | I / R / ROI → score | Estado |
|---|---|---|---|---|---|---|
| DT-01 | `.gitignore` raíz ignoraba **toda** la carpeta `joe-app/` | Regla `joe-app/` en vez de `joe-app/node_modules/` + `joe-app/dist/` | Cualquier fichero nuevo creado dentro de `joe-app/` queda invisible para git de forma silenciosa | Baja (1 línea) | 4 / 1 / 3 → 13 | **Resuelto dos veces** — arreglado en una sesión anterior cuya rama nunca se fusionó a `main` (el bug regresó); vuelto a arreglar en `claude/rax-v1-consolidacion`. No estará resuelto de verdad hasta que esta rama se fusione |
| DT-02 | Conviven `index.html` (minúscula) e `Index.html` (mayúscula) en la raíz | Evolución histórica del sitio principal sin limpiar el fichero antiguo | Confuso para cualquiera que edite el sitio; en sistemas de fichero *case-insensitive* pueden pisarse entre sí | Media (confirmar cuál es el vigente antes de tocar) | 3 / 2 / 2 → 8 | Abierto — pendiente de respuesta del propietario |
| DT-03 | Dos implementaciones paralelas de agente de WhatsApp (Meta Cloud API y Twilio) sin decidir cuál es la canónica | Se añadió Twilio como alternativa sin retirar Meta | Riesgo de mantener/depurar el webhook equivocado, o de que ambos respondan a la vez | Baja (decisión + apagar uno) | 3 / 2 / 2 → 8 | Abierto — ya no hay lógica duplicada entre los dos (`whatsapp-agent-core.js` compartido), pero la decisión de negocio sigue sin tomarse (RT-01) |
| DT-04 | Sin tests automatizados | Nunca se han escrito | Un cambio puede romper una app sin que nada lo detecte salvo el uso real | Alta (no abordado) | 3 / 2 / 1 → 7 | Abierto — CI ahora cubre lint+build (ver DT-04b), no tests |
| DT-04b | Sin CI de verificación antes de desplegar | No existía workflow de verificación, solo de despliegue | Un error de build en `alquileres`/`joe-app` solo se detectaba al fallar el deploy en Netlify | Media | 3 / 2 / 2 → 8 | **Resuelto** (`claude/rax-v1-consolidacion`): `.github/workflows/ci.yml` corre lint+build en cada PR/push |
| DT-05 | Esquemas de Supabase no centralizados | `joe-app` e `Index.html` usan proyectos Supabase independientes sin documentar cuál es cuál | Difícil saber qué proyecto usa cada app, ni reproducir el esquema | Media | 3 / 2 / 2 → 8 | Abierto |
| DT-06 | Login de `Index.html` es 100% client-side: los 4 usuarios comparten el mismo hash de contraseña por defecto | Nunca se implementó autenticación real de servidor | Sin verificar RLS (DT-07), cualquiera con la URL podría leer/escribir ventas y compras reales sin pasar por el login | Alta (3-5h con Supabase Auth) | 5 / 4 / 3 → 15 | Abierto — propietario decidió posponerlo |
| DT-07 | Estado de RLS del proyecto Supabase detrás de `Index.html` sin verificar | No hay `supabase-schema.sql` para esta app en el repo; el esquema vive solo en el dashboard de Supabase | Si RLS no está activo/bien configurado, los datos financieros reales son accesibles vía API con solo la clave publicable ya visible en el código fuente | Baja (15-30 min de verificación) | 5 / 4 / 3 → 15 | Abierto — requiere que el propietario lo verifique en el dashboard (sin acceso desde estas sesiones) |
| DT-08 | El Asistente IA de `Index.html` exponía la API key de Anthropic de cada usuario en el navegador | No se reutilizaba el patrón de proxy server-side ya existente para WhatsApp | Exposición de la key en devtools/Network tab; gasto de IA disperso sin control central | Media (1-2h) | 4 / 3 / 3 → 11 | **Resuelto** (`claude/rax-v1-consolidacion`): proxy `netlify/functions/chat-assistant.js` |
| DT-09 | `alquileres` no tenía backend real: todos los datos vivían solo en `localStorage` | La app se scaffoldeó con Supabase en mente pero nunca se conectó | Pérdida total de datos de negocio real si se borraba el navegador o se cambiaba de equipo | Alta | 5 / 3 / 4 → 15 | **Resuelto por otra vía**: `main` migró independientemente a Firebase Firestore + login por persona (más completo que el intento de Supabase de una sesión anterior, que fue descartado) |
| DT-10 | `alquileres/package.json` declara `@supabase/supabase-js` como dependencia pero no se usa en ningún fichero desde la migración a Firestore | Dependencia residual del intento de backend anterior a Firestore | Ninguno funcional; aumenta el tamaño del bundle/instalación sin motivo | Baja (quitar la línea + `npm install`) | 1 / 1 / 1 → 2 | Abierto — cambio trivial pero toca `package.json`/lockfile de una app en uso; se deja documentado en vez de tocarlo sin pedir turno |
| DT-11 | `alquileres/LoginScreen.tsx` comparte **el mismo hash de contraseña por defecto** que `Index.html` (DT-06) entre sus dos usuarios (Luis/Rober) | Mismo valor de hash copiado, no es coincidencia | Mismo tipo de riesgo que DT-06, ahora en dos apps a la vez | Alta (requiere decisión de negocio sobre credenciales) | 5 / 4 / 3 → 15 | Abierto — descubierto durante esta consolidación, no tocado (cambiar credenciales es producción) |
| DT-12 | `joe-app` y `alquileres` no tenían `eslint.config.js` (`joe-app`) o tenían errores reales nunca detectados (`alquileres`) porque nadie había podido ejecutar `npm run lint` con éxito hasta añadir el CI | Ausencia histórica de verificación automatizada | Errores reales de código (Fast Refresh roto, variables sin usar, bloques vacíos) pasaban desapercibidos indefinidamente | Baja-media (ya corregido) | 3 / 1 / 2 → 9 | **Resuelto** (`claude/rax-v1-consolidacion`): 9 errores reales corregidos entre las dos apps, ver `DECISIONES.md` |
| DT-13 | `react-hooks/set-state-in-effect` — 3 casos preexistentes en `alquileres` (`Reservations.tsx` ×2, `MigrateLocalData.tsx` ×1) | Patrón de cálculo automático de precios y de arranque de migración escrito antes de que la regla existiera | Ninguno funcional observado; el patrón puede causar renders en cascada innecesarios | Media (requiere tocar lógica de negocio activa) | 2 / 2 / 1 → 3 | Abierto, degradado a warning con TODO en `alquileres/eslint.config.js` — no se ha tocado la lógica |
| DT-14 | Sin observabilidad de errores en ninguna de las 3 apps React ni en las funciones de Netlify | Nunca se ha configurado un servicio de error tracking | Un fallo en producción solo se detecta si un usuario lo reporta a mano | Media (`@sentry/react` + `ErrorFallback.tsx`, código ya existe en `claude/autonomous-dev-environment-8obtv2`) | 3 / 2 / 2 → 8 | **Pendiente, no rescatado a propósito** — modo restringido (solo documentación) vigente desde 2026-07-12 impide implementarlo aunque el código ya esté escrito y verificado en esa rama. Además, el intento anterior de Sentry en `netlify/functions` ya se revirtió una vez por romper el deploy — si se retoma, probar aparte primero |

## Cómo añadir una entrada nueva

Cada vez que detectes deuda técnica real (no hipotética), añade una fila con
el mismo formato. No la marques `resuelto` hasta que el cambio esté
efectivamente commiteado. Antes de tocar código para resolver una entrada,
clasifica el recurso afectado como Producción / Inactivo con valor / Prueba
(ver `project-manager/SKILL.md`) y respeta esa clasificación.
