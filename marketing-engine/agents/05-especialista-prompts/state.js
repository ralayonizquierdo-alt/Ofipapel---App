// El resultado de este agente se guarda en job.state['especialista-prompts'].
const { OUTPUT_SHAPE } = require('./interface.js');

module.exports = { STATE_KEY: 'especialista-prompts', SHAPE: OUTPUT_SHAPE };
